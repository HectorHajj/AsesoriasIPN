<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tutor40">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Settings"></activity>
        <activity android:name=".PhoneLogin" />
        <activity
            android:name=".AlumnoMain"
            android:label="@string/title_activity_alumno_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TutorMaterias"
            android:label="@string/title_activity_tutor_materias"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TutorMain"
            android:label="@string/title_activity_tutor_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registro" />
        <activity android:name=".Perfil" />
        <activity
            android:name=".GroupChatActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>